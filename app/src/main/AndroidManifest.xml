<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jasch.devdroid.rxjavaexample">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.networking.NetworkingActivity" />
        <activity android:name=".ui.pagination.PaginationActivity" />
        <activity android:name=".ui.rxbus.RxBusActivity" />
        <activity android:name=".ui.search.SearchActivity"></activity>
    </application>

</manifest>